[project]
name = "x1201-exporter"
version = "0.1.0"
description = ""
authors = [
    {name = "Dominik Nedvedik",email = "dominik.nedvedik@protonmail.com"}
]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "smbus2 (>=0.5.0,<0.6.0)",
    "prometheus-client (>=0.21.1,<0.22.0)",
    "fastapi (>=0.115.11,<0.116.0)",
    "uvicorn (>=0.34.0,<0.35.0)",
    "gpiod (>=2.3.0,<3.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.group.dev.dependencies]
pytest = "^8.3.5"
requests = "^2.32.3"
pytest-mock = "^3.14.0"
